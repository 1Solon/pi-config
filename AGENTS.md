## Execution preference

Prefer inline implementation using the `executing-plans` skill.

Do not use `subagent-driven-development` or dispatch implementation
subagents unless I explicitly request it. Subagents may be used for
genuinely independent parallel research or isolated code review.
